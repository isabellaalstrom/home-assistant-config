  db_url: !secret db_url 
  purge_interval: 2
  purge_keep_days: 5
  exclude:
    domains:
      - sun
      - zwave
    entities:
      - sensor.current_version
      - sensor.version_available
      - sensor.date
      - sensor.date_time
      - sensor.time
      - sensor.nuc_containers_active
      - sensor.last_boot
  # include:
  #   entities:
  #     - device_tracker.desktop39i618g
  #     - device_tracker.isa_addon_presence
  #     - device_tracker.isabellas_iphone_x
  #     - device_tracker.isabellas_iphone_x_wifi
  #     - device_tracker.isa_laptop
  #     - device_tracker.isa_computer
  #     - device_tracker.isabellas_iphone_x_beta
  #     - sensor.isabellas_iphone_x_bt
  #     - sensor.stefan_iphone_7_bt
  #     - device_tracker.cnstefang
  #     - device_tracker.stefan_addon_presence
  #     - device_tracker.igrims_3
  #     - device_tracker.igrims
  #     - device_tracker.stefan_computer