  switch_button_card:
    !include switch-template.yaml

  detailed_info:
    !include detailed-info.yaml

  back_button:
    !include back-button.yaml

  header_text:
    !include header-template.yaml

  notification_template:
    !include notification-template.yaml

  person_more_info:
    !include person-more-info.yaml

  nest_button:
    !include nest_button.yaml

  nest_picture_button:
    !include nest_picture_button.yaml